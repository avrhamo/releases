apiVersion: v1
kind: Service
metadata:
  name: {{ include "complex-test-chart.fullname" . }}
  labels:
    {{- include "complex-test-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "complex-test-chart.selectorLabels" . | nindent 4 }}
